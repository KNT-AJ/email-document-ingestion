# Redis Production Configuration
# Extends base redis.conf with production-specific settings

include /app/config/redis.conf

# Production settings - strict security, optimized performance
loglevel notice
maxmemory 1gb

# Enable password requirement in production (set via REDIS_PASSWORD env var)
# requirepass is inherited from base config and should be set via environment

# Keep dangerous commands disabled for security
# rename-command configurations are inherited from base config
